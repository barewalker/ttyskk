# ttyskk — 端末内で完結する SKK 日本語入力

調査・設計メモ。第1段階の実装は 2026-07-25 に完了した (使い方は `README.md`)。
このファイルは「なぜこの作りにしたか」の記録として残す。

## なぜ作るか

現状は fcitx5-skk を kitty の外側 (X の入力メソッド層) で使っている。これが herdr と噛み合わない。

- herdr の prefix は `ctrl+z` (`~/.config/herdr/config.toml:63`)
- 日本語入力が有効なままペイン切り替えをすると、キーの取り合いで `Ctrl+Z` が herdr に届かず子プロセスへ素通しされる
- 結果 SIGTSTP が飛び、claude code が背後に回ってしまう

根本原因は、入力メソッドが端末エミュレータの外側にいて herdr のキー処理と調停できないこと。herdr 自身もこの問題を認識していて対症療法の設定を持つ (`switch_ascii_input_source_in_prefix`, `reveal_hidden_cursor_for_cjk_ime`) が、いずれも根治ではない。herdr はバイナリ配布でソースが手元にないため本体への組み込みは不可。

入力メソッドを端末アプリ層に持ってくれば、キーは必ず herdr → ttyskk の順に流れ、取り合いが構造的に消える。

## 方式

擬似端末で子プロセスを包む。`ttyskk -- claude` のように起動し、stdin を横取りして確定文字列だけを子に渡す。

**herdr には依存しない。** 素の kitty でも tmux でも screen でも、SSH / mosh の先でも動く汎用の端末内入力メソッド。herdr が関わるのは次の2点だけで、どちらも外せる。

1. モード表示を `herdr pane report-metadata` でペインのラベルに出す (herdr がなければカーソルの形と色で表す — `DECSCUSR` / `OSC 12`)
2. herdr の既定シェルを包む (単なる運用設定)

依存するもの: POSIX の擬似端末、VT100 系エスケープ列を解する端末。Linux / macOS。
依存しないもの: herdr, tmux, kitty 固有機能, X11 / Wayland, fcitx, D-Bus。

副次的な利点として、mosh 越しの利用ではリモート側で動くため辞書と学習履歴が NUC に集約され、どこから繋いでも同じ変換になる。

## 実装言語

Rust。単一バイナリで配布でき、`portable-pty` / `vte` が使える。`~/.cargo` は共有ホームにあるので distrobox 不要。

## 先行実装の調査

| 実装 | 言語 | 備考 |
|---|---|---|
| [sentimental-skk](https://github.com/saitoha/sentimental-skk) | Python 2 / GPL-3.0 | **画面複写 + 重ね描きを完全実装。設計図として最重要** |
| [yaskk](https://github.com/uobikiemukot/yaskk) | C / MIT | 擬似端末で包む方式。描画方式は未確認 |
| [skkfep](https://github.com/ousttrue/skkfep) | C | 名前が既に使われているので `ttyskk` を選んだ理由でもある |

ソース再取得:

```sh
curl -s https://pypi.org/pypi/sentimental-skk/json \
  | python3 -c 'import sys,json;print(next(u["url"] for u in json.load(sys.stdin)["urls"] if u["packagetype"]=="sdist"))' \
  | xargs curl -sLO
```

### sentimental-skk の描画方式 (読み解いた結果)

ここが本命。以下は展開後の `sentimental-skk-0.0.146/sskk/` からの相対パス。

**出力を二股に流す** (`sskk.py:278`)

```python
canossahandler = Canossa(screen, visibility=False)   # 画面複写を保つだけ、描画しない
outputhandler  = OutputHandler(screen, mode_handler)
multiplexer = tff.FilterMultiplexer(canossahandler, outputhandler)
```

子の出力を「そのまま端末へ素通し」と「画面模型 `Screen` へ食わせる」に分ける。`Screen` は行×セルの格子を属性込みで保持する画面の控え。これが全ての土台。

**未確定文字は子アプリに一切送らない** (`input.py:1128` `_draw_word()`)

```python
y, x = screen.getyx()                            # カーソル位置を控えから知る (CSI 6n を使わない)
output.write(u'\x1b[%d;%dH' % (y + 1, x + 1))    # そこへ直接移動
output.write(u'\x1b[0;1;4;31m' + word)           # 太字+下線+赤 = 変換対象
output.write(u'\x1b[0;1;32m' + char)             # 緑 = 未変換のローマ字
```

カーソル位置を子に問い合わせないので、往復の待ちがなく、応答が子の出力と混ざる事故も起きない。

**消すのは控えからの書き戻し** (`input.py:1158` `_draw_nothing()`)

```python
length = self._prev_length              # 前回の描画幅を覚えている
screen.copyline(output, x, y, length)   # 控えから元の内容を書き戻す
```

短くなった差分だけ復元する。右端をはみ出す場合は次の行も戻す (`input.py:1112`)。描画中は `\x1b[?25l` でカーソルを隠してちらつきを抑える。

**候補窓は上下を自動で選ぶ** (`canossa/canossa/popup.py:406`)

```python
def _getdirection(self, row):
    if row * 2 > screen.height:
        return _POPUP_DIR_REVERSE   # カーソルが下半分なら上向き
    return _POPUP_DIR_NORMAL        # 上半分なら下向き
```

fzf と同じ考え方でカーソルの真上か真下に浮かせる。**最下行に常設のステータス行を作らない** (uim-fep 系の不快な挙動を避けられる)。

**候補窓の消去** (`canossa/canossa/screen.py:1435` `dealloc()` → `copyrect()` `screen.py:1075`)

控えの各行について `CUP` で移動し、属性を再現しながら矩形を書き戻す。窓が縮んだときははみ出した部分だけを4方向それぞれ計算して復元する (`screen.py:1402` `realloc()`)。

規模は canossa 側 約9,400行、sskk 本体 約5,800行 (ローマ字規則と辞書を含む) の Python。

## 設計の骨子

- **英数時は完全透過** — 画面に1バイトも書かない
- **常設行を作らない** — 端末のスクロール領域に触らない
- **未確定文字は重ね描き** — 子アプリの画面上には一度も存在しない (副作用ゼロ)
- **候補窓はカーソルの上下に浮かせる** — 必要なときだけ

Rust での部品構成:

- 擬似端末と入出力の仲介 → `portable-pty`
- 画面の控え → 最大の山。`vte` で解析して格子を自前で持つか、`alacritty_terminal` の `Term` を借りる。復元に必要なのは文字と SGR 属性だけなので全機能の端末模倣は不要
- SKK の状態機械と辞書 → 移植ではなく素直に書き直し

段階案: 第1段階は未確定文字の重ね描きのみ (カーソル行と次の行を追えれば足りる) とし、候補一覧は行内に横並び。第2段階で格子を完全にして候補窓を浮かせる。

## 第1段階の実装で分かったこと

**格子は最初から全画面ぶん持つことにした。** カーソル行だけを追う案だったが、`ED` / `IL` / `DL` / スクロール領域 / 副画面 (DECSET 1049) を扱う以上どのみち全体が要る。`vte` に解析させて `Screen` に写す形で 500 行ほど (`src/screen.rs`)。`alacritty_terminal` は借りなかった — 復元に必要なのは文字と SGR 属性だけで、色を内部表現から SGR へ戻す手間のほうが大きい。

**「英数時は完全透過」は意識して作らないと守れない。** 素朴に書くと、未確定文字が無くても毎回カーソル位置と表示属性を書き戻してしまう。重ね描きが実際にあるときだけ割り込むようにして、`cat` / `less` / `bash -i` の出力が素の実行とバイト単位で一致することを確かめた。

**エスケープ列の途中に割り込むと壊れる。** 子の出力は読み取りの境界で `\x1b[3` / `1m` のように切れる。そこへ重ね描きのバイト列を挟むと端末が一つの壊れた列として解釈する。`vte` は自分の状態機械で途中を扱うが、素通しするバイト列のほうは別問題。末尾が未完了かを追う `SeqTracker` (`src/input.rs`) を置き、切れ目が安全なときだけ描く。

**利用者辞書は丸ごと書き出してはいけない。** herdr で複数ペインを開くと、各 ttyskk が互いの学習を消し合う。「この起動で覚えたこと」を順に記録し、保存時にディスクを読み直して重ねる方式にした。

**半端なローマ字は捨てる。** `k` を打った状態で `C-z` を押したとき、`k` を子へ送ると余計な英字が紛れ込む。バッファに残るのは必ず変換表の見出しの前半で、それ自体は文字として意味を持たないため捨ててよい (`n` だけは「ん」として確定する)。

fcitx5 の利用者辞書は送りありの見出し語が `わるs /悪/` と ttyskk と同じ規約で、注釈も `;` 区切りだったのでそのまま読めた。

**端末アプリ層にいるだけでは足りない — 鍵盤プロトコルも追う必要がある。** 「キーは必ず herdr → ttyskk → 子の順に流れる」は正しいが、*どんな形で*届くかは子アプリが決める。Claude Code は起動時に `CSI > 1 u` (kitty 鍵盤プロトコル) と `CSI > 4 ; 2 m` (modifyOtherKeys) を有効にし、その出力を ttyskk が素通しした結果、端末が `Ctrl+J` を `CSI 106;5u` として送ってくる。生の `0x0a` を待っていたのでモード切り替えが一切効かなかった。SKK が使う `Ctrl+J` / `Ctrl+G` に限って復号し、他は元のバイト列のまま渡す (`decode_extended_key`)。印字文字は「曖昧さの解消」段階では素のまま届くので、ローマ字入力そのものは影響を受けない。

**「英数時は完全透過」とモード表示は両立しない。** 使ってみると、何も打っていない状態で ttyskk が動いているのかどうか、いまどのモードなのかが分からない。変換中しか色が変わらないため。表示先の候補は、カーソルの見た目・herdr のペインラベル・画面の隅への重ね描みの三つ。常設行を作らない方針は保ったまま、**カーソルの形 (DECSCUSR) と色 (OSC 12)** を起動時から常に設定することにした。端末だけで完結して herdr に依らず、格子の内容にも触れない。ASCII を下線、かな系をブロックとしたので、形が既定と違うこと自体が「動いている」合図になる。代償として ASCII モードでも数バイト書くことになるので、完全透過が要る場面のために `TTYSKK_NO_CURSOR` を残した。子アプリがカーソルの見た目を変えたら塗り直す必要があるが、これは控えの側で DECSCUSR と OSC 12/112 を拾えば分かる。

**同じ列を出力側でも読み違えていた。** 上の `CSI > 4 ; 2 m` は端末への設定要求だが、控えの解析では終端バイトだけを見て SGR `4;2` (下線 + 薄字) と読んでいた。控えの pen が下線になり、重ね描きのあとにそれを忠実に書き戻すので、以降 Claude Code が素の文字を出すたび下線が付く。画面全体が下線だらけになって初めて気付いた。`CSI > 1 u` / `CSI < u` も `u` = カーソル復帰と誤読していて、位置と pen を勝手に巻き戻していた。**CSI は私用マーカー (`?` `>` `<` `=`) と中間バイトまで見て初めて命令が決まる。** 終端バイトだけで分岐すると、扱わないつもりの列が既存の分岐に紛れ込む。扱うと決めたもの以外は明示的に読み飛ばす形にした (`csi_dispatch` の先頭)。

**`CSI 6n` を一切使わない、は行き過ぎだった。** sentimental-skk の「カーソル位置を子に問い合わせない」を額面どおり受け取り、控えを常に (1,1) から始めていた。実際には画面の途中で起動するのが普通で、変換を始めた瞬間に重ね描きが最上段へ飛ぶ。避けたかったのは「応答が子の出力と混ざる」ことなので、**子を起こす前**なら安全に一度だけ尋ねられる。画面サイズ変更後も折り返しが組み直されて絶対位置が変わるため尋ね直すが、こちらは子の描き直しと競合するので、子の出力を処理したあとに届いた応答は古いものとして捨てる。問い合わせ中に打たれた文字は応答から分離して入力に戻す (`take_cursor_report`)。

## 環境

- 辞書: `/usr/share/skk/SKK-JISYO.L` ほか一式 (ホスト側。distrobox 内からは `/run/host/usr/share/skk/`)
- 既存のユーザー辞書: `~/.local/share/fcitx5/skk/user.dict` (EUC-JP)。学習内容を引き継げる
- fcitx5 の入力メソッド切替は `Control+space` / `Zenkaku_Hankaku` / `Alt+grave`

## 次にやること

第2段階の候補として、効きそうな順に:

1. **辞書登録** — いま候補が無いと ▽ のまま止まる。実運用で最初に困るのはここ。
2. **候補窓を浮かせる** — カーソルの上下どちらに出すかは sentimental-skk と同じくカーソルが画面の上半分か下半分かで決める。矩形の退避と復元は `Screen::restore_region` を矩形に広げれば足りる。
3. **変換中のカーソル移動** — いまは矢印キーで見出し語を確定してしまう。

## 未決事項

- 起動方法 — 全ペインで常時包むか、必要なコマンドだけか
- kitty で fcitx5 を切るタイミング (第1段階で日常の入力が回るか見てから)
